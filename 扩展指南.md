# 模块扩展与官网维护指南（ModuleRegistry / SiteCatalog）

> 适用版本：v4.7.0 起。本指南说明如何**不碰 MainWindow / HomePage / SetupPage 的核心调度代码**，只改两个数据文件就完成"加模块 / 加官网"。
> 背景：原先模块元数据散落在 `MainWindow.InitPages()` 的 `_titles`/`_factories` 字典、`HomePage.CardDict`、以及 `SetupPage.xaml` 的硬编码卡片里，人工维护要在多处同步。v4.7.0 把它们集中为 C# 实例文件，便于增删改。

---

## 一、两个文件的职责

| 文件 | 管什么 | 谁消费它 |
|---|---|---|
| `ModuleRegistry.cs` | 23 个导航模块的元数据（key / 中英文标题 / 图标 / 首页分组 / 是否主级）+ 页面实例化工厂 `CreatePage(key, host)` | `MainWindow.InitPages()`（填导航与命令面板）、`HomePage.BuildCards()`（首页卡片）、`MainWindow.BuildCommandItems()`（命令面板图标） |
| `SiteCatalog.cs` | 装机助手的"分类 → 官网条目"清单（`SiteGroup` / `SiteEntry`） | `SetupPage.BuildCatalog()`（按 `UiLanguage.Changed` 自动重渲染卡片） |

**结论**：加模块改 `ModuleRegistry.cs`；加官网改 `SiteCatalog.cs`。其它代码会自动适配。

---

## 二、新增一个导航模块（3 步）

### 1. 建页面类
新建 `XxxPage.xaml` + `XxxPage.xaml.cs`，类**必须继承 `UserControl`**（与现有 `HomePage`/`SetupPage` 等一致）：

```csharp
public partial class XxxPage : UserControl { /* ... */ }
```

### 2. 在 `ModuleRegistry.cs` 的 `All` 列表追加一条
参数顺序：`key, 中文标题, 英文标题, 图标emoji, 首页分组(可省), 是否主级(可省), 副标题中文(可省), 副标题英文(可省)`

```csharp
// 想出现在首页「效率工具」分组、常规卡片：
new("mykey", "我的工具", "My Tool", "🧩", "tools", false, "一句话功能说明", "One-line feature description"),
// 只想进侧栏、不进首页（homeGroup 留空）：
new("onlynav", "仅导航项", "Nav Only", "⭐"),
```

- **首页分组**取值：`"system"`（系统工具）/ `"tools"`（效率工具）/ `"assist"`（助手与信息）。留 `null` 或省略 = 只在侧栏/命令面板，不在首页。
- **是否主级** `isPrimary=true`：更大更显眼的卡片（目前仅系统工具组的前几个用），常规模块用 `false` 或省略。

### 3. 在 `CreatePage` 的 `switch` 加分支
```csharp
case "mykey": return new XxxPage();
```
> 注意：`CreatePage` 返回 `UserControl`，所以页面类必须是 `UserControl`（见第一节）。

**到此即可**。`MainWindow.InitPages()`、`HomePage.BuildCards()`、`BuildCommandItems()` 会遍历 `All` 自动注册，无需手动改字典。

---

## 三、让新页面能"跳转 / 返回 / 打开其它页"

部分页面需要行为能力（例如首页卡片点了跳到某个模块、设置页"完成"后返回首页）。做法是：**页面声明回调属性 + 工厂里用对象初始化器把 `host` 方法接上**。

可用的宿主回调（来自 `INavigationHost`）：

| host 方法 | 含义 |
|---|---|
| `host.Navigate(string key)` | 跳转到指定模块 |
| `host.Optimize()` | 触发顶部「一键优化」 |
| `host.CloseToHome()` | 返回首页 |
| `host.OpenTutorial()` | 打开 AI 密钥教程页 |
| `host.OpenAgent()` | 打开 Agent 助手页 |

**页面侧**声明可空 Action 属性（沿用现有命名约定）：
```csharp
public Action<string>? OnNavigate { get; set; }
public Action?        OnCloseRequest { get; set; }
```
页面内部在按钮点击等时机调用，例如 `OnNavigate?.Invoke("clean");`。

**工厂侧**接线（参照现有写法）：
```csharp
case "mykey": return new XxxPage { OnNavigate = host.Navigate, OnCloseRequest = host.CloseToHome };
```
> 需要回调却忘了接线 → 按钮点了"没反应"，这是最常见的坑。

---

## 四、调整首页卡片（分组 / 顺序 / 主级）

首页卡片由 `HomePage.BuildCards()` 遍历 `ModuleRegistry.All` 生成，**顺序即 `All` 列表里的顺序**：
- 换分组：改该条的 `homeGroup`（`"system"`/`"tools"`/`"assist"`）。
- 调顺序：直接在 `All` 列表里移动那一行。
- 改主级大卡片：`isPrimary` 置 `true`。
无需改动任何 XAML。

---

## 五、装机助手新增官网 / 分类（只改 SiteCatalog.cs）

### 加条目到已有分类
在对应 `new SiteGroup(...)` 内部追加一条 `SiteEntry`：
```csharp
new SiteEntry("🟦 某软件", "中文一句话描述", "English one-line description", "https://example.com"),
```

### 加一个全新分类
复制一个 `SiteGroup` 块（注意第二个参数是**英文**标题，后面用 `params` 收尾若干 `SiteEntry`）：
```csharp
new("🗂 新分类中文", "🗂 New Category EN",
    new SiteEntry("🅰 条目A", "描述", "Desc", "https://a.com"),
    new SiteEntry("🅱 条目B", "描述", "Desc", "https://b.com")),
```

**无需改 `SetupPage.xaml` / `SetupPage.xaml.cs`** —— `BuildCatalog()` 会遍历 `Groups` 自动生成卡片、分组标题，并在切换语言时自动重渲染。

字段说明：
- `Name`：显示名称，建议带 emoji 图标。
- `DescZh` / `DescEn`：简短描述，分别用于中文 / 英文界面。
- `Url`：必须带 `https://` 的完整网址；点击卡片即打开该 URL。

---

## 六、注意事项 / 踩坑清单

1. **key 全局唯一**：`Find(key)` 按 key 线性查找，重复会命中第一条，导致导航错乱。
2. **页面必须继承 `UserControl`**：否则 `CreatePage` 无法将其作为 `UserControl` 返回（编译报错）。
3. **需要行为的页面务必接线回调**：在 `CreatePage` 的对应 `case` 用对象初始化器把 `host.X` 赋给页面的 `OnXxx` 属性，否则点了没反应。
4. **描述文案**：英文避免单/双引号（与 `LocExtension` 同理，避免解析问题）；中文可用全角标点，不会被误切。
5. **改完必编译**：`dotnet build -c Release` 确认 0 错误 0 警告。XAML / 枚举值错误 **编译不报错、运行才崩**（`XamlParseException` 写 `%LOCALAPPDATA%/WINHELP/crash.log`），发布前建议跑一次 headless 自检（启动 exe 约 12s 后结束，查 crash.log 有无新条目）。

---

## 七、字段速查

```csharp
// ModuleRegistry.cs
ModuleDefinition(string key, string titleZh, string titleEn, string icon,
                 string? homeGroup = null, bool isPrimary = false,
                 string? subtitleZh = null, string? subtitleEn = null)

// SiteCatalog.cs
SiteEntry(string name, string descZh, string descEn, string url)
SiteGroup(string titleZh, string titleEn, params SiteEntry[] items)
```
