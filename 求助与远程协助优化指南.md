# 求助与远程协助优化指南（面向开发与维护）

> 适用版本：v5.6.0。本文替代早期「求助家人」按钮方案：把"如何求助"沉淀为**文档 + 手册**，
> 而不是占用首页 UI。这样界面更简洁，同时家人/技术人员仍能获得完整、可维护的求助路径。

---

## 一、为什么要做成文档而不是功能

v5.6.0 曾计划在首页加入「需要帮忙？向家人求助」按钮（一键生成求助说明 + 打开远程协助下载页）。
评审后决定**移除该 UI 功能**，理由：

1. **与「电脑帮助」模块定位重叠**：求助说明本质上是一份文档，放在 `PcHelpPage` 的「使用技巧」里更自然；
2. **按钮收益低**：老人遇到问题时的真实诉求是"找个人"，而"生成 txt 发给家人"需要家人配合，链路太长；
3. **维护成本**：远程协助工具官网、操作步骤会变，硬编码在代码里要随版本发布，文档则随时可改。

结论：**求助路径 = 软件内「电脑帮助」文字说明 + 本指南（开发用）**。

---

## 二、给新手用户的求助路径（写入「电脑帮助」即可）

### 2.1 远程协助工具推荐（按易用性排序）

| 工具 | 适用人群 | 特点 | 官网 |
| --- | --- | --- | --- |
| Windows 快速助手（Quick Assist） | 家庭用户 | Windows 10/11 自带，无需安装；家人只需 Microsoft 账户 | 开始菜单搜索「快速助手」 |
| ToDesk | 国人家庭 | 免费、速度快、手机电脑互控 | https://www.todesk.com/ |
| RustDesk | 注重隐私 | 开源免费，可选自建服务器 | https://rustdesk.com/zh-cn/ |
| QQ / 微信远程 | 已有账号 | 无需额外注册，但功能较少 | 客户端内发起 |

### 2.2 三步求助法（给新手写进帮助页）

1. **先把情况说清楚**：打开「一键体检」→ 导出报告（txt），把报告发给家人；
   或打开「系统状况」截图硬件信息页。
2. **装一个远程协助软件**：家人和孩子装同一个（推荐 ToDesk 或快速助手）。
3. **把设备代码告诉家人**：孩子输入代码即可远程查看并操作电脑；
   **只给信任的人**，用完后在软件里退出或关闭"允许远程控制"。

### 2.3 安全提示（务必写进帮助页）

- 远程协助**只需要对方提供的设备代码/邀请码**，绝不要把自己的 Windows 密码告诉对方；
- 结束后在远程软件里点「断开」并退出；
- 不要点开陌生人发来的链接或软件安装包；
- 若家人发现电脑被陌生人远程操作，立即拔网线 / 断开 WiFi 并重启电脑。

---

## 三、开发侧的优化清单（想做成内置功能时的实施建议）

如果未来要把「求助」做成内置功能，建议按以下优先级（比 v5.6.0 的按钮方案更完整）：

### P0：一键生成「电脑概况」文件（复用现有能力）
- 复用 `CheckupPage` 的体检报告导出（已是 txt，含硬件/健康度/建议），
  `PcHelpPage` 加一个「导出电脑概况」按钮即可，**不要**新写一份 WMI 采集逻辑。

### P1：远程协助软件检测 + 直达
- 检测本机是否已装 ToDesk / RustDesk / 快速助手（进程名 + 安装目录）；
- 已装 → 一键启动；未装 → 打开官网下载页（用 `SafeUrl.Open`，URL 走编译期常量白名单）；
- 注意：**不要内置任何远程工具安装包**（体积 + 安全审查成本）。

### P2：求助入口位置
- 优先放在 `PcHelpPage` 顶部（而不是首页横幅），与"使用技巧"并列；
- 首页欢迎条保持克制：问候 + 统计 + 一键优化即可。

### P3：不要做的
- ❌ 云端中转/中转服务器（隐私风险，GPL 项目不做）；
- ❌ 内置远程桌面实现（工程量巨大，且已有成熟免费工具）；
- ❌ 自动上传系统信息（违背"本地运行、不上传"的隐私承诺）。

---

## 四、相关文件

- 远程协助下载/官网白名单：`SafeUrl.cs`（新增 URL 需为编译期常量 + `Open` 校验）
- 电脑帮助页（求助步骤落点）：`PcHelpPage.xaml / PcHelpPage.xaml.cs`
- 一键体检报告（电脑概况来源）：`CheckupPage.xaml.cs`
- 版本：本指南随 v5.6.0 归档；若未来在「电脑帮助」落地求助步骤，请同步更新本文第二节文案。
