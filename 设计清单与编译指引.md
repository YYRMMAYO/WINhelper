# WINHELP 2.3.0 优化 — 设计清单与代码编译指引

> 适用范围：WPF 桌面软件 WINHELP（`net10.0-windows`，WinExe）。
> 本文档基于 2.3.0 的实际落地方案，给出**可逐条核对的的设计清单**与**可复现的编译/发布流程**。
> 设计取向：分区清晰、层级明确、操作安全（绝不误删用户文件）、无花哨干扰元素。

---

## 一、设计目标与原则

| 目标 | 说明 |
| --- | --- |
| 信息架构升级 | 用「左侧导航 + 右侧分区卡片」替代单列表，降低认知负担 |
| 新增实用功能 | 5 个电脑帮助类功能：系统清理 / 启动项 / 网络诊断 / 故障向导 / 新手导览 |
| 配置透明 | 设置页标注开发者与非盈利声明 |
| 版本与发布闭环 | 统一版本号 2.3.0，输出独立 exe + 安装包 |
| 操作安全 | 清理只删临时文件；启动项禁用用可逆子项，不删除注册表项 |

**视觉原则（对照干净设计准则）**
- 单一强调色，统一圆角与阴影，避免高饱和渐变与"AI 味"装饰。
- 固定间距栅格（卡片 220×160，圆角 14，统一阴影），保持节奏一致。
- 文字对比度达标：主文 `#2C3E50`、次级 `#7F8C8D`（已优于旧版 `#95A5A6`）。

---

## 二、设计清单（按模块，可逐条核对）

### 模块 A：整体框架与导航

| # | 设计项 | 设计意图 | 实现位置 | 状态 |
| --- | --- | --- | --- | --- |
| A1 | 左侧导航栏 | 9 个固定入口（主界面/系统清理/启动项/网络诊断/故障向导/新手导览/设置/主题/陪伴运行），点击平滑切换右侧内容 | `MainWindow.xaml` 左侧 `Border` + `NavItemStyle` | ✅ |
| A2 | 右侧分区卡片 | 按「常用工具 / 系统与健康 / 帮助与支持」三段分组 | `MainWindow.xaml` `ContentPanel` 内 3 个 `TextBlock` 标题 + `WrapPanel` | ✅ |
| A3 | 顶部状态栏 | CPU / 内存 / 网络实时指标 + "一键优化"入口 | `MainWindow.xaml` `StatusPill` + `BtnOptimize` | ✅ |
| A4 | 底部每日贴士 | 侧栏底部轮换使用贴士 | `MainWindow.xaml` `TxtDailyTip` + `BtnNextTip` | ✅ |
| A5 | 卡片悬停反馈 | 鼠标悬停上浮 + 阴影增强 | `MainWindow.xaml.cs` `AttachCardHover()` | ✅ |

### 模块 B：视觉与主题系统

| # | 设计项 | 设计意图 | 实现位置 | 状态 |
| --- | --- | --- | --- | --- |
| B1 | 强调色单例 | 全局共享 `SolidColorBrush`，换肤即时生效 | `ThemeManager.AccentColor` + `ApplyButtonTheme` | ✅ |
| B2 | 卡片样式 | 白色表面、圆角 14、柔和阴影 | `MainWindow.xaml` `HomeCardStyle`（`CardShadow` Blur20/Opacity0.08） | ✅ |
| B3 | 图标圆角底 | 每类功能配色块 + emoji 图标 | 各 `Card*` 内 `Border` 背景色 | ✅ |
| B4 | 窗口图标统一 | 所有子窗口套用同一图标 | `ThemeManager.SetWindowIcon(this)` | ✅ |

### 模块 C：五大新功能（Window10–Window14）

| # | 功能 | 入口 | 核心逻辑 | 安全/边界 | 状态 |
| --- | --- | --- | --- | --- | --- |
| C1 | 🧹 系统清理 · C 盘 | `Window10`（`CardClean`/`BtnClean`） | `BtnScan_Click` 扫描 `%TEMP%` 临时文件 + 桌面/下载/文档/视频/图片中 >200MB 大文件；`BtnClean_Click` 仅删除临时文件；`Fmt()` 字节格式化 | **只删临时文件，绝不自动删用户大文件**（大文件仅展示） | ✅ |
| C2 | 🚀 启动项管理 | `Window11`（`CardStartup`/`BtnStartup`） | 读取 `HKCU\…\Run` + `HKLM\…\Run`；`Toggle()` 把值搬到 `WINHELP_Disabled` 子项 | 禁用=迁移而非删除，**可逆可恢复**；捕获 HKLM 管理员权限异常 | ✅ |
| C3 | 📡 网络诊断 | `Window12`（`CardNet`/`BtnNet`） | `GetIsNetworkAvailable()` → 枚举 up 网卡与速率 → `Ping 223.5.5.5`（2000ms）→ `Dns.GetHostAddresses("www.baidu.com")`；`AddRow()` 输出 | 只读探测，不改网络配置 | ✅ |
| C4 | 🔧 故障排查向导 | `Window13`（`CardWizard`/`BtnWizard`） | 静态 `Dictionary<string,List<string>>` 6 类（无法上网/电脑卡顿/蓝屏重启/没有声音/软件闪退/风扇吵发热）；`RenderCategories()` → `RenderSolution()` 步骤 + 返回 | 纯指引，无自动修改系统 | ✅ |
| C5 | 📘 新手导览 | `Window14`（`CardNovice`/`BtnNovice`） | 12 条小白常用技巧（Win+D 显示桌面、Ctrl+Shift+Esc 任务管理器、Win+E 资源管理器、Win+Shift+S 截图、Win+V 剪贴板历史、Win+I 设置、Alt+Tab 切换等）按日轮换；`BtnNextTip` 切换 | 纯阅读，无副作用 | ✅ |

### 模块 D：设置 — 开发者信息 + 非盈利声明

| # | 设计项 | 设计意图 | 实现位置 | 状态 |
| --- | --- | --- | --- | --- |
| D1 | 开发者信息 | 标注 "开发者：YYRMM" | `Window4.xaml`「关于」区（纯 XAML，无需后端改动） | ✅ |
| D2 | 非盈利声明 | 绿色提示条："本软件完全免费，不盈利，仅供学习与交流使用，请勿用于商业用途。" | `Window4.xaml` `Border` 背景 `#E8F5E9` 文字 `#2E7D32` | ✅ |

### 模块 E：版本统一 2.3.0

| # | 文件 | 改动 | 状态 |
| --- | --- | --- | --- |
| E1 | `WINHELP.csproj`（根） | `<Version>2.2.0</Version>` → `2.3.0` | ✅ |
| E2 | `_build/WINHELP.csproj` | `<Version>1.8.0</Version>` → `2.3.0` | ✅ |
| E3 | `1.9.0pre01.iss` | `MyAppVersion "1.9.0"` → `2.3.0`；`OutputBaseFilename` → `WINHELP_Setup_v2.3.0`；新增 `OutputDir=F:\new\WINHELP\dist\BAND` | ✅ |

---

## 三、视觉与交互规范（Token 表，可供 Ardot 画布还原参考）

| 类别 | Token | 值 |
| --- | --- | --- |
| 背景 | App 背景 | `#F0F2F5` |
| 背景 | 侧栏 | `#F7F9FB` |
| 强调 | 主强调色 | `#4A90D9`（按钮）/ `#2E86C1`（选中态） |
| 文字 | 主文 | `#2C3E50` |
| 文字 | 次级 | `#7F8C8D` |
| 文字 | 弱化 | `#95A5A6`（副标题）、`#BDC3C7`（版本号） |
| 表面 | 卡片 | 白 `#FFFFFF`，圆角 `14`，阴影 Blur20/Depth2/Opacity0.08 |
| 状态 | 成功绿 | `#27AE60` / `#E8F5E9` |
| 状态 | 警示橙 | `#FF9800` / `#FFF3E0` |
| 间距 | 卡片外距 | `Margin 0,0,14,14` |
| 字号 | 区段标题 | 15 / Bold |
| 字号 | 卡片标题 | 15 / Bold |
| 字号 | 卡片副文 | 12 / `#7F8C8D` |

---

## 四、代码编译指引

### 4.1 环境要求

| 依赖 | 版本 / 路径 | 校验命令 |
| --- | --- | --- |
| .NET SDK | 10.0.301（`net10.0-windows`） | `dotnet --version` |
| Inno Setup | 6，安装于 `C:\Program Files (x86)\Inno Setup 6\ISCC.exe` | `ls "/c/Program Files (x86)/Inno Setup 6"` |
| 系统 | Windows x64 | — |

### 4.2 关键路径表

| 用途 | 路径 |
| --- | --- |
| 主工程 | `F:\new\WINHELP\WINHELP.csproj` |
| 发布输出（单文件自包含） | `F:\new\WINHELP\bin\Release\net10.0-windows\win-x64\publish\WINHELP.exe` |
| 交付 exe | `F:\new\WINHELP\dist\WINHELP.exe` |
| 冒烟测试读取路径 | `F:\new\WINHELP\_build\bin\Release\net10.0-windows\win-x64\WINHELP.exe` |
| 安装包脚本 | `F:\new\WINHELP\1.9.0pre01.iss` |
| 安装包产物 | `F:\new\WINHELP\dist\BAND\WINHELP_Setup_v2.3.0.exe` |
| 冒烟测试工程 | `F:\new\WINHELP\_smoketest\SmokeTest.csproj`（`Program.cs`） |

### 4.3 编译与发布（单文件自包含）

```bash
cd F:/new/WINHELP
dotnet publish WINHELP.csproj -c Release -r win-x64
```

产物：`bin/Release/net10.0-windows/win-x64/publish/WINHELP.exe`（约 73MB，自包含，无需目标机安装 .NET 运行时）。

### 4.4 复制到交付 / 测试路径

冒烟测试从 `_build\bin\...` 读取 exe，需先放置；同时准备 `dist` 交付件：

```bash
cd F:/new/WINHELP
cp "bin/Release/net10.0-windows/win-x64/publish/WINHELP.exe" dist/WINHELP.exe
cp "bin/Release/net10.0-windows/win-x64/publish/WINHELP.exe" "_build/bin/Release/net10.0-windows/win-x64/WINHELP.exe"
```

### 4.5 可行性验证（冒烟测试）

```bash
cd F:/new/WINHELP/_smoketest
dotnet run -c Release
```

判定（输出 `PASS` 条件）：`baseline_main_visible` / `companion_visible` / `main_hidden` / `main_visible_after_exit` 四项均为 `True`，且 `hotkey_registered` 非空（形如 `Ctrl+Shift\``）。

> 说明：`App.xaml.cs` 内置一个**仅当测试环境变量生效**的轻量调试钩子（`WINHELP_COMPANION_AUTO`、`WINHELP_HOTKEY_DEBUG`）。普通双击启动不会进入该分支，**对正常使用零影响**。测试采用轮询等待主窗口真正可见，避免自包含 exe 冷启动慢导致的误判。

### 4.6 Inno Setup 打包安装程序

```bash
"/c/Program Files (x86)/Inno Setup 6/ISCC.exe" "F:/new/WINHELP/1.9.0pre01.iss"
```

产物：`F:\new\WINHELP\dist\BAND\WINHELP_Setup_v2.3.0.exe`（约 68MB）。脚本已固化：源 `dist/WINHELP.exe`、输出 `dist/BAND`、发布者 `YYRMM`、版本 `2.3.0`、图标 `AppIcon.ico`。

---

## 五、编译中已排掉的坑（避免重复踩）

| 问题 | 现象 | 修复 |
| --- | --- | --- |
| `Thickness` 构造 | 仅支持 1 参/4 参，原 `new Thickness(14,12)` 编译报错 | 全部改为 4 参 `new Thickness(14,12,14,12)`（Window10–13） |
| 缺命名空间 | Window10 用到 `Colors` 报找不到 | 补 `using System.Windows.Media;` |
| 自包含 exe 冷启动慢 | 原 2s 基线过早，主窗口未可见即判失败 | 冒烟测试改为轮询 `IsVisible`，并据此安排陪伴窗口进入/退出 |
| 测试 env 变量原未被读取 | 原 `SmokeTest` 依赖的 `WINHELP_COMPANION_AUTO` 等从未被 App 使用 | 在 `App.xaml.cs` 增加"仅测试环境变量生效"的钩子 |

---

## 六、交付物清单

| 文件 | 说明 |
| --- | --- |
| `F:\new\WINHELP\dist\WINHELP.exe` | 独立运行程序（自包含，约 73MB） |
| `F:\new\WINHELP\dist\BAND\WINHELP_Setup_v2.3.0.exe` | 安装包（约 68MB） |

---

## 七、若需在 Ardot 画布还原该设计（可选）

上述 Token 表（第三节）可直接映射到 Ardot 的设计变量：强调色 `#4A90D9`、卡片圆角 14、阴影柔和、三段式 Bento 分区。风格建议选 **clean / minimal / productivity**，避免高饱和渐变与 AI 装饰元素，保持与现有 WPF 实现一致。
