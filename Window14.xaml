<UserControl x:Class="WINHELP.Window14"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">

    <UserControl.Resources>
        

        <Style x:Key="TipCard" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="18"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="Effect" Value="{StaticResource GlassShadow}"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Border Style="{StaticResource GlassCard}" Grid.Row="0" Height="56" BorderBrush="#EDF0F3" BorderThickness="0,0,0,1">
            <Grid Margin="20,0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Width="34" Height="34" CornerRadius="10" Background="#EBF3FC" VerticalAlignment="Center">
                        <TextBlock Text="📘" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center" Margin="12,0,0,0">
                        <TextBlock Text="新手导览" FontSize="16" FontWeight="Bold" Foreground="#2C3E50"/>
                        <TextBlock Text="小白也能看懂的 司南工具箱 使用指南" FontSize="11" Foreground="#7F8C8D"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 内容 -->
        <ScrollViewer Grid.Row="1" Margin="20,16" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- 欢迎语 -->
                <Border Style="{StaticResource TipCard}">
                    <StackPanel>
                        <TextBlock Text="👋 欢迎使用 司南工具箱" FontSize="17" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,10"/>
                        <TextBlock FontSize="13" Foreground="#7F8C8D" TextWrapping="Wrap" LineHeight="22">
                            司南工具箱 是一款面向电脑小白的日常工具箱。左侧导航栏可以快速进入各个功能，
                            主页卡片则把常用入口都摆在眼前。遇到困难时，记得试试「故障排查向导」和「Agent 助手」。
                        </TextBlock>
                    </StackPanel>
                </Border>

                <!-- 功能导览 -->
                <TextBlock Text="主要功能一览" FontSize="14" FontWeight="Bold" Foreground="#2C3E50" Margin="4,8,0,12"/>

                <Border Style="{StaticResource TipCard}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Width="44" Height="44" CornerRadius="10" Background="#E8F5E9" Margin="0,0,14,0">
                            <TextBlock Text="🧹" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="系统清理 · C 盘" FontSize="14" FontWeight="SemiBold" Foreground="#2C3E50"/>
                            <TextBlock Text="帮你扫描临时文件、找出大文件，安全清理释放空间。" FontSize="12" Foreground="#7F8C8D" TextWrapping="Wrap" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Style="{StaticResource TipCard}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Width="44" Height="44" CornerRadius="10" Background="#FFF3E0" Margin="0,0,14,0">
                            <TextBlock Text="🚀" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="启动项管理" FontSize="14" FontWeight="SemiBold" Foreground="#2C3E50"/>
                            <TextBlock Text="查看开机自启程序，禁用不必要的项目，让电脑开机更快。" FontSize="12" Foreground="#7F8C8D" TextWrapping="Wrap" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Style="{StaticResource TipCard}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Width="44" Height="44" CornerRadius="10" Background="#E1F5FE" Margin="0,0,14,0">
                            <TextBlock Text="📡" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="网络诊断" FontSize="14" FontWeight="SemiBold" Foreground="#2C3E50"/>
                            <TextBlock Text="一键检测网络连通性、Ping 延迟、DNS 解析，快速定位网络问题。" FontSize="12" Foreground="#7F8C8D" TextWrapping="Wrap" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Style="{StaticResource TipCard}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Width="44" Height="44" CornerRadius="10" Background="#EDE7F6" Margin="0,0,14,0">
                            <TextBlock Text="🔧" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="故障排查向导" FontSize="14" FontWeight="SemiBold" Foreground="#2C3E50"/>
                            <TextBlock Text="选择你遇到的问题，按步骤提示自行排查，省时省力。" FontSize="12" Foreground="#7F8C8D" TextWrapping="Wrap" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- 每日贴士专区 -->
                <TextBlock Text="每日使用贴士" FontSize="14" FontWeight="Bold" Foreground="#2C3E50" Margin="4,16,0,12"/>
                <Border Style="{StaticResource TipCard}" Background="#FFFBEA">
                    <StackPanel>
                        <TextBlock x:Name="TxtTip" FontSize="14" Foreground="#2C3E50" TextWrapping="Wrap" LineHeight="22"
                                   Text="按 Win+D 可快速显示桌面，再次按可恢复窗口。"/>
                        <Button x:Name="BtnNextTip" Content="换一条贴士" Height="32" Width="110" Margin="0,14,0,0"
                                FontSize="12" FontWeight="SemiBold" Foreground="White" Background="#4A90D9"
                                BorderThickness="0" Cursor="Hand" HorizontalAlignment="Left"
                                Click="BtnNextTip_Click"/>
                    </StackPanel>
                </Border>

                <!-- 常用快捷键 -->
                <TextBlock Text="常用快捷键" FontSize="14" FontWeight="Bold" Foreground="#2C3E50" Margin="4,16,0,12"/>
                <WrapPanel>
                    <Border Style="{StaticResource GlassCard}" CornerRadius="10" Padding="14" Width="240" Margin="0,0,12,12">
                        <StackPanel>
                            <Grid Margin="0,0,0,6">
                                <TextBlock Text="窗口操作" FontSize="13" FontWeight="SemiBold" Foreground="#2C3E50"/>
                                <Button HorizontalAlignment="Right" Content="📋 复制" FontSize="11"
                                        Foreground="#4A90D9" Background="Transparent" BorderThickness="0" Cursor="Hand"
                                        Padding="6,2" VerticalAlignment="Center"
                                        Tag="Win+D 显示桌面&#x0a;Alt+Tab 切换窗口&#x0a;Win+↑/↓ 最大化/还原"
                                        Click="BtnCopyShortcuts_Click"/>
                            </Grid>
                            <TextBlock FontSize="12" Foreground="#7F8C8D" TextWrapping="Wrap">
                                Win+D 显示桌面<LineBreak/>
                                Alt+Tab 切换窗口<LineBreak/>
                                Win+↑/↓ 最大化/还原
                            </TextBlock>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource GlassCard}" CornerRadius="10" Padding="14" Width="240" Margin="0,0,12,12">
                        <StackPanel>
                            <Grid Margin="0,0,0,6">
                                <TextBlock Text="文件与搜索" FontSize="13" FontWeight="SemiBold" Foreground="#2C3E50"/>
                                <Button HorizontalAlignment="Right" Content="📋 复制" FontSize="11"
                                        Foreground="#4A90D9" Background="Transparent" BorderThickness="0" Cursor="Hand"
                                        Padding="6,2" VerticalAlignment="Center"
                                        Tag="Win+E 资源管理器&#x0a;Win+S 搜索&#x0a;Ctrl+Shift+N 新建文件夹"
                                        Click="BtnCopyShortcuts_Click"/>
                            </Grid>
                            <TextBlock FontSize="12" Foreground="#7F8C8D" TextWrapping="Wrap">
                                Win+E 资源管理器<LineBreak/>
                                Win+S 搜索<LineBreak/>
                                Ctrl+Shift+N 新建文件夹
                            </TextBlock>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource GlassCard}" CornerRadius="10" Padding="14" Width="240" Margin="0,0,12,12">
                        <StackPanel>
                            <Grid Margin="0,0,0,6">
                                <TextBlock Text="截图与工具" FontSize="13" FontWeight="SemiBold" Foreground="#2C3E50"/>
                                <Button HorizontalAlignment="Right" Content="📋 复制" FontSize="11"
                                        Foreground="#4A90D9" Background="Transparent" BorderThickness="0" Cursor="Hand"
                                        Padding="6,2" VerticalAlignment="Center"
                                        Tag="Win+Shift+S 截图&#x0a;Win+V 剪贴板历史&#x0a;Ctrl+Shift+Esc 任务管理器"
                                        Click="BtnCopyShortcuts_Click"/>
                            </Grid>
                            <TextBlock FontSize="12" Foreground="#7F8C8D" TextWrapping="Wrap">
                                Win+Shift+S 截图<LineBreak/>
                                Win+V 剪贴板历史<LineBreak/>
                                Ctrl+Shift+Esc 任务管理器
                            </TextBlock>
                        </StackPanel>
                    </Border>
                </WrapPanel>
            </StackPanel>
        </ScrollViewer>

        <!-- 底部 -->
        <Border Style="{StaticResource GlassCard}" Grid.Row="2" Height="56" BorderBrush="#EDF0F3" BorderThickness="0,1,0,0">
            <Grid Margin="20,0">
                <TextBlock x:Name="TxtStatus" Text="有问题随时打开「故障排查向导」或「电脑帮助」" FontSize="12"
                           Foreground="#7F8C8D" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                <Button x:Name="BtnDone" Content="我知道了" Height="36" Width="120"
                        FontSize="13" FontWeight="SemiBold" Foreground="White" Background="#4A90D9"
                        BorderThickness="0" Cursor="Hand" HorizontalAlignment="Right"
                        Click="BtnDone_Click"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
